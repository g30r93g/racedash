// Placeholder — actual handler code deployed by cloud-auth from apps/api/dist/lambda.handler
export const handler = async () => ({
  statusCode: 200,
  body: JSON.stringify({ status: 'placeholder' }),
})
