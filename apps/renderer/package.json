{
  "name": "@racedash/renderer",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prebuild": "pnpm --filter @racedash/core build",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm --filter @racedash/core build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "remotion": "remotion",
    "preview": "remotion preview src/index.ts"
  },
  "dependencies": {
    "@racedash/core": "workspace:*",
    "@racedash/timestamps": "workspace:*",
    "@remotion/google-fonts": "^4.0.434",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "remotion": "^4.0.0"
  },
  "devDependencies": {
    "@remotion/cli": "^4.0.434",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "tslib": "^2.8.1",
    "typescript": "*",
    "vitest": "*"
  }
}
