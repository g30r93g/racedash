import { describe, it } from 'vitest'

describe('CreditAdjustmentForm', () => {
  it.todo('submits grant with positive rcAmount')
  it.todo('submits correction with negative rcAmount')
  it.todo('validates reason is required')
  it.todo('disables submit while request is in-flight')
})
