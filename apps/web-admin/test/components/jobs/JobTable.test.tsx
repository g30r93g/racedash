import { describe, it } from 'vitest'

describe('JobTable', () => {
  it.todo('renders rows for each job')
  it.todo('shows empty state when no jobs')
  it.todo('triggers pagination callback on next page')
})
