import { describe, it } from 'vitest'

describe('useAdminAuth', () => {
  it.todo('returns isAdmin true when user has admin role')
  it.todo('returns isAdmin false when user lacks admin role')
  it.todo('returns isLoading true while auth state is resolving')
})
